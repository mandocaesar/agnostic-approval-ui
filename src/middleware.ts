// Middleware disabled - authentication handled in layout.tsx
// Empty matcher ensures this middleware never runs

export function middleware() {
  // No-op
}

export const config = {
  matcher: [],
};
